export default function AppService() {
    'ngInject';
    // var baseUrl = '/assets/data';
    return {
        // getCountries: (success, error) => {
        //     $http.get('/countries.json').then(success, error);
        // },
        //
        // getCities: (success, error) => {
        //     $http.get('/cities.json').then(success, error);
        // }
    }
}
